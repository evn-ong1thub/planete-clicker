Non
jte promet que c est oui

//evan test 

<div>
        <h2>Boutique</h2>
        <p id="amelioration-1">Amélioration 1</p>
    </div>

    <!-- Compteurs & boutons -->
    <h1 id="stonecountertext">Count: <span id="count">0</span></h1>
    
    <h2 id="woodcountertext">Wood count: <span id="woodcount">0</span></h2>
    <button id="planete-centrale">PLANETE CENTRALE</button>
    <button id="planete-de-bois">PLANETE DE BOIS</button>
    
    <script>
        var clickCount = 0;

        // Quand on clique sur le bouton, on augmente les compteurs
        facile.onClick('#planete-centrale', () => {
            clickCount = clickCount + 1
            if (clickCount == 100 ) {
                facile.show('#woodcountertext')
            }
            
            facile.write("#count", clickCount)
            console.log("Change click count: ", clickCount)
        })

        facile.write('#amelioration-1', 'Améliorer planète 1');
       
        var clickCountwood = 0;
        facile.onClick('#planete-de-bois', () => {
            clickCountwood = clickCountwood + 1 
            facile.write("#woodcount", clickCountwood)
        })
        //on cache les compteurs et les boutons 
        facile.hide('#planete-de-bois');
          facile.hide('#woodcountertext')
        
            
          // Quand on clique sur le bouton, on augmente les compteurs
  // facile.onClick('Planet-Pierre', () => {
  //     clickCount = clickCount + 1
 //     if (clickCount == 100 ) {
  //         facile.show('#Planet-Bois')
 //     }
            
 //     facile.write("#count", clickCount)
 //     console.log("Change click count: ", clickCount)
 // })
           
    


        
           
        

    </script>
    var clickCountWater = 0;
        facile.onClick('#Planet-Eau', () => {
            clickCountWater = clickCountWater + 1
            facile.write("#countEau", clickCountWater)
         } )

    
</body>

</html>

//cacher les compteurs 
facile.hide('#Planet-Bois')     
 facile.hide('#Planet-Eau')
  facile.hide('#Planet-Charbon')
    facile.hide('#Planet-Fer')
     facile.hide('#Planet-Uranium')
      facile.hide('#Planet-Obsidienne')
       facile.hide('#Planet-Diamant')
//
       var clickCountWater = 0;
        facile.onClick('#Planet-Eau', () => {
            clickCountWater = clickCountWater + 1
            facile.write("#countEau", clickCountWater)
        });

        <div id="upgradestone"> <button id='upgrade1'>UPGRADE STONE</button></div>
 
//condition.
 && clickCountStone.value == 20





 facile.onClick('#upgradestone', () => {
    if (clickCountStone.value >= 75 ) { 
 clickCountStone.value = clickCountStone.value - 75}
})
clickCountStone.onChange((newCount) => {
    facile.write('#countStone', newCount);
});

facile.onClick('#upgradewood', () => {
    if (clickCountWood.value >= 75 ) { 
 clickCountWood.value = clickCountWood.value - 75}
})
clickCountStone.onChange((newCount) => {
    facile.write('#countWood', newCount);
});